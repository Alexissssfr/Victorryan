services:
  - type: web
    name: victorryan
    env: node
    buildCommand: npm install
    startCommand: node render-start.js
    envVars:
      - key: NODE_ENV
        value: production
    buildFilter:
      paths:
        - backend/**
        - frontend/**
        - package.json
        - render-start.js
